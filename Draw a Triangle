def draw_triangle():
    height = int(input("Enter the height of the triangle: "))
    for i in range(height):
        print(' ' * (height - i - 1) + '*' * (2 * i + 1))

# Example usage
draw_triangle()
