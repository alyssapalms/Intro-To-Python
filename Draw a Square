def draw_square():
    size = int(input("Enter the size of the square: "))
    for row in range(size):
        if row == 0 or row == size - 1:
            print('*' * size)
        else:
            print('*' + ' ' * (size - 2) + '*')

draw_square()
